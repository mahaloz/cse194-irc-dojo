#!/opt/pwn.college/python

import subprocess

with open("/home/hacker/challenge/thisworks.txt", "w") as file:
    file.write("flag")

with open("/home/hacker/thisworks.txt", "w") as file:
    file.write("flag")

command = ["cd", "/challenge/sic-1.3/"]
subprocess.run(command, text=True)

command = ["sudo", "make"]
subprocess.run(command, text=True)

command = ["sudo", "make", "install"]
subprocess.run(command, text=True)