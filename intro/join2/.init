#!/opt/pwn.college/python

import subprocess
import pathlib

dir = pathlib.Path(__file__).parent

commands = ["cd " + dir / "sic-1.3", "sudo make", "sudo make install"]

for command in commands:
    subprocess.run(command, shell=True, check=True)