#!/opt/pwn.college/python

import subprocess
import pathlib

dir = pathlib.Path(__file__).parent

with open(dir / "thisworks.txt", "w") as file:
    file.write("flag")

with open("/home/hacker/thisworks.txt", "w") as file:
    file.write("flag")

command = ["cd", dir  / "sic-1.3"]
subprocess.run(command, text=True)

command = ["sudo", "make"]
subprocess.run(command, text=True)

command = ["sudo", "make", "install"]
subprocess.run(command, text=True)